extends layout

block main
  main
    h1.main-heading Error #{error.status} 
      span #{message}
    .decorative-svg.zoom200
      include ../public/medias/images/silos/131v-d1.svg
    pre
      code(translate='no' data-select='Select and copy') #{error.stack}
    hr
    include includes/links
